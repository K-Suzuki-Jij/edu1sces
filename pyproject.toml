[build-system]
requires = ["maturin>=1.10"]
build-backend = "maturin"

[project]
name = "edu1sces"
version = "0.1.0"
requires-python = ">=3.9"

[tool.maturin]
python-source = "python"
module-name = "edu1sces.core"
