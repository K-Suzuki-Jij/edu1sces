# Claude Code Configuration for edu1sces

## Running Tests

To avoid PyO3 Python linking issues when running tests, activate the virtual environment first:

```bash
source .venv/bin/activate
cargo test
```

Or run with the specific Python path:
```bash
VIRTUAL_ENV=.venv cargo test
```
